{
  "name": "playwright-ai-healer-tests",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "test": "playwright test",
    "test:headed": "playwright test --headed",
    "test:report": "playwright show-report",
    "test:json": "playwright test --reporter=json"
  },
  "devDependencies": {
    "@playwright/test": "^1.50.0",
    "typescript": "^5.6.0"
  }
}
